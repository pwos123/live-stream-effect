# 直播间自动触发特效播放实现原理

主要是把一个透明前端页面作为 Overlay 放进直播伴侣、直播姬、OBS 浏览器源或平台内嵌 WebView；后端监听直播间事件，按规则判断要播放的特效，再把播放指令实时推送给 Overlay。Overlay 负责素材加载、队列调度、动画播放和性能控制，最终由推流软件或平台客户端合成到直播画面中。

 

## 1. 主流方案

| 方案类型                  | 代表形态                                                     | 典型触发事件                                 | 核心实现方式                                               | 适用场景                           |
| ------------------------- | ------------------------------------------------------------ | -------------------------------------------- | ---------------------------------------------------------- | ---------------------------------- |
| 官方开放平台/直播伴侣插件 | 抖音直播伴侣插件、B 站直播姬能力、快手直播伴侣能力、平台合作接口 | 弹幕、礼物、点赞、关注、粉丝团、进房、PK     | 平台提供事件通道，插件或服务接入后渲染 Overlay             | 商业项目、合规要求高、需要稳定权限 |
| 本地弹幕姬/桥接服务       | B 站弹幕姬、平台直播伴侣插件、本地弹幕服务                   | 弹幕、礼物、点赞、舰长/会员、粉丝团          | 本地程序接收直播间消息，再转发给 Overlay 或后端            | 桌面直播间、个人主播、快速验证玩法 |
| 自研浏览器 Overlay        | Vue/React/原生 JS 页面 + 直播伴侣/直播姬/OBS 浏览器源        | 礼物金额、弹幕关键词、点赞里程碑、粉丝团等级 | 后端统一事件协议，经 WebSocket 推送给前端播放特效          | 品牌直播间、多平台适配、强定制视觉 |
| 平台内互动玩法            | 小程序、小游戏、平台 WebView、互动组件                       | 任务、连麦、投票、抽奖、礼物互动             | 在平台受控容器中运行前端，使用平台组件或 Canvas/WebGL 渲染 | 平台内闭环玩法、轻量互动、活动运营 |
| 游戏化互动引擎            | Unity/Unreal/Godot/HTML5 游戏                                | 礼物生成角色、弹幕控制游戏、点赞触发技能     | 后端把直播事件转成游戏指令，渲染画面再被推流软件捕获       | 虚拟主播、礼物互动游戏、复杂 2D/3D |

关键：

(1) 优先接入官方开放能力、平台直播伴侣插件或平台合作接口。

(2) 对不同平台事件做统一归一化，不让前端直接依赖平台原始字段。

(3) 规则引擎只输出播放意图，由 Overlay 决定具体播放方式。

(4) 高频事件合并、限流和降级，避免点赞、弹幕、低价礼物把画面刷爆。

(5) 素材、规则、房间、主播权限可配置，便于运营后台管理。

  

## 2. 平台事件接入

直播平台常见事件包括：

(1) 弹幕：普通弹幕、关键词弹幕、表情弹幕、管理员弹幕。

(2) 礼物：礼物 ID、礼物名称、数量、连击、价值、用户信息。

(3) 点赞：单次点赞、累计点赞、点赞里程碑。

(4) 关注：新关注、回关、粉丝团加入。

(5) 粉丝团/会员：粉丝团等级、舰长/会员、续费、升级。

(6) 进房：观众进入直播间、老粉回流、高等级用户进房。

(7) 互动：连麦、PK、红包、投票、任务、抽奖。

接入方式通常有三类：

| 接入方式              | 说明                                     | 优点                 | 注意事项                               |
| --------------------- | ---------------------------------------- | -------------------- | -------------------------------------- |
| 官方开放平台/合作接口 | 通过平台授权、回调或长连接获取事件       | 合规性和稳定性最好   | 需要申请权限、审核资质、遵守平台规则   |
| 直播伴侣/直播姬插件   | 插件运行在平台客户端或指定容器中         | 与平台客户端结合紧密 | 容器能力、审核流程和发布方式受平台限制 |
| 本地弹幕姬/桥接服务   | 本地程序接收消息，再转发到后端或 Overlay | 开发快，适合验证玩法 | 需要考虑稳定性、合规边界和多房间管理   |

也有抓包、未授权逆向接口或绕过平台风控的方式，直播业务对稳定性和合规性要求高，非官方路径容易出现掉线、限流、封禁、字段变更和审核风险。

 

## 3. Overlay 前端播放层

(1) 透明背景：页面 CSS 设置。

(2) 固定画布：需要适配画布。

(3) 分层渲染：全屏礼物、弹窗、弹幕、点赞、角标分别放不同层。

(4) 素材预加载：直播开始前加载高频礼物和点赞素材。

(5) 队列控制：区分独占特效可叠加特效可合并特效。

(6) 对象池：点赞飘屏、弹幕雨、金币雨不要无限创建 DOM。

(7) 音频策略：在直播伴侣、直播姬、OBS 浏览器源内分别测试自动播放、音量和采集链路。

  

## 4. 特效文件格式

| 格式/技术             | 文件格式           | 播放方式                      | 优点                                 | 缺点                                       | 适合场景                             |
| --------------------- | ------------------ | ----------------------------- | ------------------------------------ | ------------------------------------------ | ------------------------------------ |
| WebM 透明视频         | .webm              | HTML <video>，VP8/VP9 + Alpha | 还原度高，适合复杂光效；浏览器源友好 | 文件较大；移动端兼容需测试；动态换文字困难 | 全屏礼物、爆炸、烟花、转场           |
| MP4 视频              | .mp4               | HTML <video> 或媒体源         | 兼容性强、压缩率好                   | 常规 H.264 MP4 不带透明 Alpha              | 无透明要求的视频、背景动画           |
| GIF                   | .gif               | <img>                         | 使用简单，兼容性好                   | 体积大、颜色少、无音频、透明边缘差         | 小表情、低成本贴纸                   |
| APNG/WebP 动图        | .png/.webp         | <img>                         | 比 GIF 质量好，支持透明              | 播放控制弱                                 | 小型循环动效、图标                   |
| Lottie                | .json/.lottie      | lottie-web / player           | 矢量、体积小、可改文字和颜色         | 复杂 AE 特性可能失真                       | 关注弹窗、任务进度、图标动画         |
| SVGA                  | .svga              | SVGA Player                   | 直播礼物特效常见；可替换头像和昵称   | 格式生态较小；复杂效果有限                 | 礼物动效、头像昵称嵌入、App 风格特效 |
| Spine                 | .json/.skel+.atlas | Runtime / WebGL               | 骨骼动画强，适合角色动作             | 制作成本高，需要授权                       | 虚拟角色、宠物、召唤物               |
| Rive                  | .riv               | WASM / Canvas / WebGL         | 状态机和交互输入强                   | 设计工具和运行时需学习                     | 可交互任务、状态机礼物               |
| Canvas 2D             | 无固定格式         | JS 绘制                       | 轻量、动态能力强                     | 复杂光效性能有限                           | 点赞飘屏、弹幕雨、简单粒子           |
| WebGL/PixiJS/Three.js | 纹理/JSON/Shader   | GPU 渲染                      | 性能强，可做大量粒子和 3D            | 开发调试成本高，容器兼容需测               | 高并发粒子、3D 礼物、互动游戏        |

 

## 5. 合成方式

### 5.1 直播伴侣/直播姬浏览器源

Overlay 作为透明网页加载到平台客户端中。

优点：

(1) 与平台直播工具链结合紧密。

(2) 前端热更新方便，支持动态文字、头像、礼物数量。

(3) 可以复用 WebSocket、Canvas、WebGL、Lottie、SVGA 等前端能力。

(4) 注意事项：

(5) 不同平台客户端的 WebView 能力不同，需实测 WebGL、音频、透明背景和跨域加载。

### 5.2 OBS 浏览器源

 OBS 推流到抖音、B 站、快手等平台。

(1) 前端开发灵活，透明网页叠加稳定。

(2) 对视频、Canvas、WebGL、SVG、Lottie、音频支持较好。

(3) 可通过本地服务或后端网关实时接收播放指令。

(4) 浏览器源的缓存、刷新、音频采集和硬件加速需要单独调试。

### 5.3 媒体源/窗口捕获/虚拟摄像头

固定视频特效、Unity/Unreal/Godot 游戏化互动、虚拟主播使用。

(1) 单个视频素材播放稳定。

(2) 复杂 2D/3D 场景可独立渲染，再被推流软件捕获。

(3) 多素材编排、音频路由、透明背景和窗口捕获链路更复杂。

  

## 6. 接入资料准备清单

接入准备：

(1) 抖音、B 站、快手、视频号、小红书等目标平台的最新官方开放文档或合作接口说明。

(2) 平台允许获取的事件类型、字段范围、频率限制和回调方式。

(3) 直播伴侣、直播姬或平台 WebView 对透明背景、音频、WebGL、跨域资源的支持情况。

(4) 礼物、弹幕、点赞、粉丝团等事件的测试房间和测试账号。

## 7. 本仓库已实现的无 CDK 可运行方案

在没有抖音平台 CDK、开放平台回调或直播伴侣插件权限时，本仓库采用“本地 OCR 监听 + 透明浏览器 Overlay”的方案：

1. 主播在电脑上正常使用抖音直播伴侣或抖音直播管理页开播，并保持互动消息区域可见。
2. 打开本地 `monitor.html`，通过浏览器屏幕共享权限选择包含互动消息的窗口或屏幕。
3. `monitor.html` 使用 OCR 识别可见文字，按常见文案解析为 `enter`、`like`、`comment`、`gift` 等统一事件。
4. 本地 Node 服务把事件通过 Server-Sent Events 推送给 `overlay.html`。
5. `overlay.html` 是透明背景页面，放进 OBS 或抖音直播伴侣“浏览器源/网页源”后，特效会被合成进推流画面，所以抖音直播间观众可以看见自定义特效。

这个方案不抓包、不逆向、不调用非公开接口，只读取主播本人屏幕上已经可见的互动文字；稳定性取决于 OCR 清晰度、直播伴侣互动区字号、窗口遮挡情况和机器性能。商业项目仍建议优先申请官方能力。

### 7.1 运行步骤

```bash
npm install
npm start
```

启动后会看到三个本地地址：

- `http://127.0.0.1:8787/overlay.html`：透明特效层，添加到 OBS 或抖音直播伴侣浏览器源。
- `http://127.0.0.1:8787/monitor.html`：抖音互动消息 OCR 监听器。
- `http://127.0.0.1:8787/control.html`：事件测试台，用于不直播时验证特效播放。

### 7.2 抖音直播伴侣配置步骤

1. 运行 `npm start`。
2. 在抖音直播伴侣中添加“浏览器源/网页源”，URL 填写 `http://127.0.0.1:8787/overlay.html`。
3. 设置浏览器源尺寸为直播画布尺寸，例如 1920×1080，并置于最上层。
4. 打开浏览器访问 `http://127.0.0.1:8787/control.html`，点击测试事件，确认直播伴侣预览画面出现特效。
5. 开播后打开 `http://127.0.0.1:8787/monitor.html`，点击“开始屏幕识别”，选择抖音直播伴侣或直播管理页中包含互动消息的窗口。
6. 保持互动消息区域不要被遮挡；当用户进入直播间、点赞、评论、刷礼物时，OCR 监听器会自动触发 Overlay 特效。

### 7.3 OBS 配置步骤

1. OBS 添加“浏览器”来源，URL 填写 `http://127.0.0.1:8787/overlay.html`。
2. 宽高设置为推流分辨率，例如 1920×1080。
3. 勾选透明背景相关设置（OBS 浏览器源默认支持透明网页），并把该来源放到场景最上层。
4. 使用 `control.html` 先测试，再用 `monitor.html` 对抖音直播伴侣或直播管理页互动区做 OCR 监听。

### 7.4 调优建议

- 抖音互动区字号尽量调大，避免透明背景、花字或低对比度主题。
- 只共享互动消息所在窗口，减少 OCR 干扰。
- 礼物、点赞等高频事件已做本地去重；如果画面仍过载，可在 `server.js` 中调大去重时间。
- 如果 CDN 无法加载 OCR 库，`control.html` 和 `overlay.html` 仍可运行；生产环境可以把 Tesseract.js 下载到本地后改成相对路径。

### 7.5 抖音直播间为什么能看到特效

抖音直播间观众看到的不是 `http://127.0.0.1:8787/overlay.html` 这个本地页面本身，而是主播端推流软件最终合成出来的视频画面。也就是说：

`抖音互动消息 → monitor.html OCR 识别 → 本地 Node 服务 → overlay.html 播放透明特效 → OBS/抖音直播伴侣把 Overlay 合成到直播画面 → 推流到抖音 → 直播间观众看到特效`

所以关键不是让抖音服务器访问 `127.0.0.1`，而是把 `overlay.html` 作为“浏览器源/网页源”放到你正在推流的画布里。只要你在推流软件预览画面里能看到特效，抖音直播间观众通常就能看到同样的特效。

### 7.6 推荐接入方式：优先用抖音直播伴侣网页源

如果你的账号没有 OBS 推流码或不确定是否有第三方推流权限，推荐直接使用抖音直播伴侣：

1. 启动本地程序：`npm start`。
2. 打开抖音直播伴侣，正常登录并创建直播。
3. 在直播伴侣素材/来源里添加“网页源/浏览器源”（不同版本名称可能略有差异）。
4. URL 填写：`http://127.0.0.1:8787/overlay.html`。
5. 宽高设置为直播画布大小，例如 `1920×1080` 或 `1280×720`。
6. 把该网页源拖到最上层，确保不会被摄像头、窗口采集、图片素材遮挡。
7. 打开 `http://127.0.0.1:8787/control.html` 点一次测试事件；如果直播伴侣预览区出现特效，说明观众端也会跟随推流看到。
8. 开播后再打开 `http://127.0.0.1:8787/monitor.html`，选择抖音直播伴侣/直播管理页里互动消息区域所在窗口开始 OCR 监听。

这种方式不需要 OBS，也不需要抖音推流码。它的本质是让抖音直播伴侣自己完成“摄像头/画面 + 特效网页源”的合成并推送到抖音。

### 7.7 什么时候使用 OBS 推流

OBS 不是必须的，只有在以下情况才建议使用：

- 你的抖音账号已经开通 OBS/RTMP 推流能力，能拿到服务器地址和推流密钥。
- 你已经习惯用 OBS 做多机位、复杂场景、虚拟摄像头、音频混流或绿幕合成。
- 抖音直播伴侣的网页源能力不足，或者你需要更灵活的浏览器源控制。

OBS 有两种常见链路：

1. **OBS 直接推流到抖音**：在 OBS 里添加浏览器源 `overlay.html`，再使用抖音提供的 RTMP 地址和推流密钥开播。这个方式要求账号具备第三方推流权限。
2. **OBS 虚拟摄像头 → 抖音直播伴侣**：在 OBS 里完成所有画面合成并开启虚拟摄像头，然后在抖音直播伴侣里选择 OBS Virtual Camera 作为摄像头来源。这个方式适合没有推流码但想继续使用 OBS 场景能力的情况；最终仍由抖音直播伴侣开播。

### 7.8 判断是否接入成功

按下面顺序检查：

1. 浏览器打开 `control.html` 点击测试，`overlay.html` 页面本身能播放特效。
2. 抖音直播伴侣或 OBS 的预览画面里能看到同一个特效。
3. 开播后用另一台手机或另一个抖音账号进入直播间，确认直播画面里出现特效。
4. 再启动 `monitor.html` 做 OCR 监听，让真实进房、点赞、评论、礼物自动触发特效。

如果第 1 步成功但第 2 步失败，说明网页源没有正确添加或层级/尺寸不对；如果第 2 步成功但第 3 步失败，通常是推流画面选择错误、虚拟摄像头没选对，或直播伴侣没有采集到包含特效的场景。
